package fi.finlit.edith.dto

//remove if not needed
import scala.collection.JavaConversions._

object OrderBy {

  class OrderBy {
  }

  val KEYTERM = new OrderBy()

  val LEMMA = new OrderBy()

  val USER = new OrderBy()

  val STATUS = new OrderBy()

  val DATE = new OrderBy()
}
